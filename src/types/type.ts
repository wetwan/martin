export type Navlink ={
    label: string,
    link: string
}
export type ProdcutItem={
    id: string;
    name: string;
    colors: string[];
    images: string[];
    types: string;
    shapes: string;
    category: string;
    bestDesign?: boolean;
    newDesign?: boolean;
  }